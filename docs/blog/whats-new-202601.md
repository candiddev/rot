---
author: Candid Development
date: 2026-01-20
description: Release notes for Rot v2026.01.
tags:
  - release
title: "What's New in Rot: v2026.01"
type: blog
---

## Deprecations

## Features

## Enhancements

- Updated dependencies to latest versions.
- Renamed CLI commands, the old names should still work via command matching:
  - `alg` is now `algorithms`
  - `hash-ver` is now `hash-verify`
  - `jwt-dis` is now `jwt-display`
  - `key-add-prv` is now `key-add-private`
  - `key-add-public` is now `key-add-public`
  - `key-del-prv` is now `key-delete-private`
  - `key-del-pub` is now `key-delete-public`
  - `key-dis` is now `key-display`
  - `key-dis-pub` is now `key-display-public`
  - `key-lst` is now `key-list`
  - `keyring-del` is now `keyring-delete`
  - `keyring-lst` is now `keyring-list`
  - `rand` is now `random`
  - `sig-new` is now `signature-new`
  - `sig-ver` is now `signature-verify`
  - `ssh-dis` is now `ssh-display`
  - `val-add` is now `value-add`
  - `val-add-prv` is now `value-add-private`
  - `val-cpy` is now `value-copy`
  - `val-del` is now `value-delete`
  - `val-dis` is now `value-display`
  - `val-lnk` is now `value-link`
  - `val-lst` is now `value-list`
  - `val-mov` is now `value-move`
  - `val-set-meta` is now `value-set-meta`
  - `x509-dis` is now `x509-display`

## Fixes

## Removals

- Removed support for uppercase environment variables.
